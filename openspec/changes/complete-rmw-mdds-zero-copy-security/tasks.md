## 1. Current-State Audit And RED Gates

- [x] 1.1 Trace the current loaned-message and security paths and record why the broader goal is still incomplete.
- [x] 1.2 Add a failing zero-copy host test or fake-backend contract that detects the current loaned publish call to ROS-to-MDDS serialization and any payload-copy fallback.
- [x] 1.3 Add a failing zero-copy host test or fake-backend contract that detects the current loaned take call to MDDS-to-ROS deserialization and any ROS-owned storage fallback.
- [x] 1.4 Add a failing security-required test or contract that proves current `rmw_mdds_cpp` does not fail closed or enforce policy when ROS security is required.

## 2. True Zero-Copy Loaned Messages

- [x] 2.1 Identify the MDDS bridge/backend ownership API needed to keep one loan/sample buffer across publish, transport, and take.
- [x] 2.2 Implement true zero-copy publish for the first supported fixed-size message shape without `MessageAdapter::EncodeMddsIntoBuffer` on the loaned path.
- [x] 2.3 Implement true zero-copy take for the first supported fixed-size message shape without `MessageAdapter::DecodeMdds` on the loaned path.
- [x] 2.4 Return explicit unsupported behavior for loaned message shapes that cannot be safely represented as zero-copy MDDS samples.
- [x] 2.5 Preserve existing bounded-copy compatibility tests for non-loaned publish/take and unsupported loaned shapes.

## 3. Security-Required Runtime Behavior

- [x] 3.1 Define how `rmw_mdds_cpp` detects ROS security-required mode from init options and environment/configuration.
- [x] 3.2 Implement fail-closed behavior when security is required but SROS2 policy or secure transport enforcement is unavailable.
- [x] 3.3 If full security support is implemented, add authorized and unauthorized host tests for SROS2 policy enforcement.
- [x] 3.4 If full security support is implemented, add board-side positive and negative security evidence over MDDS transport.

## 4. Verification And Handoff

- [x] 4.1 Run focused host tests for the new zero-copy and security gates.
- [x] 4.2 Run `ohos/test_rmw_mdds_delivery_contracts.sh` and confirm the new gate markers are included.
- [x] 4.3 Re-run the affected native MDDS board lane after zero-copy or security behavior changes.
- [x] 4.4 Update this task file with command evidence and exact remaining blockers.
- [x] 4.5 Perform the final audit and leave the active goal incomplete unless zero-copy and security are closed with evidence or explicitly accepted as out of scope.

## Audit Notes

- 2026-07-03 code-graph audit: `rmw_publish_loaned_message` still calls `MessageAdapter::EncodeMddsIntoBuffer` before `BridgeBackend::PublishLoaned`, and bridge loaned take still calls `MessageAdapter::DecodeMdds` before returning the ROS loaned message.
- 2026-07-03 code-graph audit: security handling currently copies/finalizes `rmw_security_options`, stores and reports enclaves, and exposes type identity, but no SROS2 policy enforcement, keystore handling, or secure transport path was found.
- 2026-07-03 outcome: the completed `complete-rmw-mdds-feature-closure` change remains valid for scoped runtime closure, but the broader "perfect/all ROS 2 features" goal is still incomplete until this follow-up closes or explicitly scopes out zero-copy and security.
- 2026-07-03 security RED test: after adding `RmwMddsPubSub.SecurityEnforceInitFailsClosedWithoutPolicyEnforcement`, `cmake --build build/rmw_mdds_cpp --target test_pubsub_inproc -j4 && source install/setup.bash && build/rmw_mdds_cpp/test_pubsub_inproc --gtest_filter="RmwMddsPubSub.SecurityEnforceInitFailsClosedWithoutPolicyEnforcement"` failed because `rmw_init` returned `RMW_RET_OK`, left context fields non-null, and did not set the expected security unsupported error.
- 2026-07-03 security implementation: `rmw_init` now rejects `options->security_options.enforce_security == RMW_SECURITY_ENFORCEMENT_ENFORCE` before context allocation/startup and returns `RMW_RET_UNSUPPORTED` with a clear security-not-supported error.
- 2026-07-03 security GREEN test: after rebuilding and installing `rmw_mdds_cpp`, `source install/setup.bash && build/rmw_mdds_cpp/test_pubsub_inproc --gtest_filter="RmwMddsPubSub.SecurityEnforceInitFailsClosedWithoutPolicyEnforcement"` passed 1/1.
- 2026-07-03 focused regression: `source install/setup.bash && ctest --test-dir build/rmw_mdds_cpp -R "^test_pubsub_inproc$" --output-on-failure` passed 1/1.
- 2026-07-03 delivery update: `ohos/test_rmw_mdds_delivery_contracts.sh` now runs the focused fail-closed test and emits `RESULT|rmw_mdds_security_enforce_fail_closed|PASS`.
- 2026-07-03 delivery verification: `bash ohos/test_rmw_mdds_delivery_contracts.sh` passed with the security marker, package CTest 22/22, upstream `test_rmw_implementation` 16/16, type-description PASS, host CLI pub/sub/service/action/params/lifecycle/graph/QoS/transient-local/message-info PASS markers, and final `rmw_mdds_delivery_contracts_ok`.
- 2026-07-03 transient verification note: one delivery run failed once in the pre-existing lifecycle host CLI probe because `/lc_talker` did not appear, then `bash ohos/tools/run_rmw_mdds_host_cli_lifecycle.sh` passed in isolation and the full delivery contract rerun passed. No code change was made for that transient discovery/startup miss.
- 2026-07-03 zero-copy RED contract: `bash ohos/test_rmw_mdds_zero_copy_contracts.sh` failed as expected with five current blockers: loaned publish still uses `MessageAdapter::EncodeMddsIntoBuffer`, still borrows a second payload-only bridge loan through `BorrowLoanedSample`, loaned take still uses `MessageAdapter::DecodeMdds`, `TryTakeBridgeLoanedMessage` still allocates ROS-owned storage, and `TakeLoanedCommon` still falls back to ROS-owned allocation.
- 2026-07-03 zero-copy implementation: `MessageAdapter::SupportsRawLoanedMessage()` now limits loaned messages to flat fixed-size scalar types; publisher/subscription `can_loan_messages` is advertised only for those types; `rmw_borrow_loaned_message` constructs the first supported `std_msgs/msg/Int32` typed object at the start of the bridge loan; `rmw_publish_loaned_message` publishes that same loan with `MessageSize()` bytes and no encode or second loan; `TryTakeBridgeLoanedMessage` returns the bridge loan payload pointer directly and records it for return; unbounded `std_msgs/msg/String` loaned publish/take is explicitly unsupported while normal copy/serialized/dynamic takes remain covered.
- 2026-07-03 zero-copy GREEN verification: `bash ohos/test_rmw_mdds_zero_copy_contracts.sh` passed with `RESULT|rmw_mdds_zero_copy_contracts|PASS`; `cmake --build build/rmw_mdds_cpp --target test_bridge_loaned_rmw test_bridge_loaned_take_rmw -j4` passed; build-space `test_bridge_loaned_rmw` passed 9/9 and `test_bridge_loaned_take_rmw` passed 9/9 with `LD_LIBRARY_PATH=build/rmw_mdds_cpp:install/lib:$LD_LIBRARY_PATH`.
- 2026-07-03 package/upstream regression: after `cmake --install build/rmw_mdds_cpp`, `ctest --test-dir build/rmw_mdds_cpp --output-on-failure` required absolute `PYTHONPATH=/home/kaihong/ros2/install/lib/python3.12/site-packages` when run outside `install/setup.bash`; with that environment it passed 22/22. `ctest --test-dir build/test_rmw_implementation -R __rmw_mdds_cpp$ --output-on-failure` passed 16/16.
- 2026-07-03 delivery verification after zero-copy: `bash ohos/test_rmw_mdds_delivery_contracts.sh` passed with security, zero-copy, artifact, package CTest 22/22, upstream `test_rmw_implementation` 16/16, type-description, host CLI pub/sub/service/action/params/lifecycle/graph/QoS/transient-local/message-info markers, and final `rmw_mdds_delivery_contracts_ok`.
- 2026-07-03 OHOS build/deploy: `./ohos/colcon_rk3588a.sh rmw_mdds_cpp` rebuilt the board overlay; `ohos/tools/deploy_rmw_mdds_delta.sh <rk3588a-board-a> <rk3588a-board-b>` deployed `librmw_mdds_cpp.so` sha `5a8411b4f454571386c5df869a29fe30e6d9efb6f897230e7ff536a6839b798c`, broker sha `85f5c7afc3c51f4bd98cad1c2e8f1edd3df968e6124b3c6e2435feac3680670f`, and bridge sha `82e49a4c8e01df25a3980376ff5c24290bf4cded1c08a59c36d047c547e9e452` to both boards.
- 2026-07-03 board verification: `ohos/tools/run_cross_board_rmw_mdds_m2m.sh <rk3588a-board-a> <rk3588a-board-b>` passed with `RESULT|m2m_pubsub_std_msgs_string|PASS|received=40/40 exact(RELIABLE zero-loss)`, `RESULT|m2m_service_add_two_ints|PASS|sum=42`, `RESULT|m2m_action_fibonacci|PASS|status=SUCCEEDED;sequence=0,1,1,2,3,5;feedback_msgs=4`, `M2M_SUMMARY pass=3 fail=0`, and `cross_board_rmw_mdds_m2m_ok`. Local `hdc` printed post-command segfault noise, but explicit board markers passed.
- 2026-07-03 final audit: zero-copy publish/take for the first flat fixed-size shape, unsupported unbounded loaned shapes, fail-closed security-required init, host delivery, OHOS build/deploy, and m2m board evidence are closed. The broad "all ROS 2 features/perfect" goal should still not be marked complete unless full positive/negative SROS2 policy enforcement is explicitly accepted as out of scope or implemented and verified.
- 2026-07-03 SROS2 policy RED follow-up: added disabled host tests `RmwMddsPubSub.DISABLED_Sros2PolicyAllowsAuthorizedPublisherAndSubscription` and `RmwMddsPubSub.DISABLED_Sros2PolicyRejectsUnauthorizedPublisher`, plus `ohos/test_rmw_mdds_sros2_policy_contracts.sh`, to make the remaining full-security requirement executable without breaking normal CTest. `cmake --build build/rmw_mdds_cpp --target test_pubsub_inproc -j4` passed.
- 2026-07-03 SROS2 policy RED verification: `bash ohos/test_rmw_mdds_sros2_policy_contracts.sh` executed both disabled tests and reported `RESULT|rmw_mdds_sros2_policy_contracts|RED|full_sros2_policy_enforcement_missing`; both tests currently fail at `rmw_init` with `ROS security enforcement is not supported by rmw_mdds_cpp; refusing insecure init`. This keeps tasks 3.3 and 3.4 open.
- 2026-07-03 regression after SROS2 RED contract: `build/rmw_mdds_cpp/test_pubsub_inproc --gtest_filter=RmwMddsPubSub.SecurityEnforceInitFailsClosedWithoutPolicyEnforcement` passed 1/1 with `LD_LIBRARY_PATH=/home/kaihong/ros2/build/rmw_mdds_cpp:/home/kaihong/ros2/install/lib`; `bash ohos/test_rmw_mdds_security_contracts.sh` passed with `rmw_mdds_security_contracts_ok`; `bash ohos/test_rmw_mdds_zero_copy_contracts.sh` passed with `RESULT|rmw_mdds_zero_copy_contracts|PASS`.
- 2026-07-03 SROS2 host policy implementation: `rmw_init` now keeps fail-closed behavior for security-required mode without a readable policy, but accepts a readable local `governance.xml`/`permissions.xml` policy root and stores publish/subscribe grants in the context. `rmw_create_publisher` and `rmw_create_subscription` enforce those topic grants before endpoint creation. This is host policy enforcement only; signed SROS2 artifact validation, transport crypto, and board-side positive/negative evidence remain open under 3.4.
- 2026-07-03 SROS2 host policy GREEN verification: `cmake --build build/rmw_mdds_cpp --target test_pubsub_inproc -j4` passed; `bash ohos/test_rmw_mdds_sros2_policy_contracts.sh` passed with `RESULT|rmw_mdds_sros2_policy_contracts|PASS`; the original fail-closed test `RmwMddsPubSub.SecurityEnforceInitFailsClosedWithoutPolicyEnforcement` passed 1/1; `bash ohos/test_rmw_mdds_security_contracts.sh` passed with `rmw_mdds_security_contracts_ok`; `bash ohos/test_rmw_mdds_zero_copy_contracts.sh` passed with `RESULT|rmw_mdds_zero_copy_contracts|PASS`.
- 2026-07-03 SROS2 host policy regression: a first full `ctest --test-dir build/rmw_mdds_cpp --output-on-failure` run failed in `test_lifecycle` because the test binary was stale after `rmw_context_impl_s` layout changed; rebuilding `test_lifecycle` and then `cmake --build build/rmw_mdds_cpp -j4` fixed the ABI mismatch. Isolated `RmwMddsLifecycle.ContextOwnsRtpsParticipantFromInit` and `RmwMddsLifecycle.ContextRetainsRemoteSpdpParticipants` then passed.
- 2026-07-03 delivery verification after SROS2 host policy: after `cmake --install build/rmw_mdds_cpp`, `bash ohos/test_rmw_mdds_delivery_contracts.sh` passed with `RESULT|rmw_mdds_sros2_policy_contracts|PASS`, security, zero-copy, artifact, package CTest 22/22, upstream `test_rmw_implementation` 16/16, type-description, host CLI pub/sub/service/action/params/lifecycle/graph/QoS/transient-local/message-info markers, and final `rmw_mdds_delivery_contracts_ok`.
- 2026-07-03 board SROS2 policy harness: added `ohos/tools/run_cross_board_rmw_mdds_sros2_policy.sh`, which creates a temporary SROS2 keystore on both boards, runs authorized cross-board `std_msgs/String` pub/sub over `rmw_mdds_cpp` and DSoftBus/MDDS, and verifies unauthorized publisher creation is denied before data is sent.
- 2026-07-03 board SROS2 build/deploy: `./ohos/colcon_rk3588a.sh rmw_mdds_cpp` rebuilt the OHOS overlay; `ohos/tools/deploy_rmw_mdds_delta.sh <rk3588a-board-a> <rk3588a-board-b>` deployed `librmw_mdds_cpp.so` sha `3b723276e01024d9de7ded08f3aa0f691ae59447f5d99784cd82053a1965e78b`, broker sha `85f5c7afc3c51f4bd98cad1c2e8f1edd3df968e6124b3c6e2435feac3680670f`, and bridge sha `82e49a4c8e01df25a3980376ff5c24290bf4cded1c08a59c36d047c547e9e452` to both boards.
- 2026-07-03 board SROS2 verification: `ohos/tools/run_cross_board_rmw_mdds_sros2_policy.sh <rk3588a-board-a> <rk3588a-board-b> 94` passed with `RESULT|board_sros2_authorized_pubsub|PASS|topic=/mdds_sros2_allowed|received=20`, `RESULT|board_sros2_unauthorized_publish|PASS|topic=/mdds_sros2_forbidden|denied`, and `cross_board_rmw_mdds_sros2_policy_ok`.
- 2026-07-06 security status correction: the 2026-07-03 board SROS2 verification above covers local XML policy with `NONE` protection only. It does not certify signed protected SROS2 traffic by itself. The later domain-221 protected signed SROS2 rerun exposed a real DSoftBus encryption lookup bug: the protected transport probe activated on both RK3588A boards, but authorized protected pub/sub returned `received=0`; hilog showed repeated `GetEncryptByChannelId` session-not-found, `CheckSocketEncrypted` encrypted=0 after retry, protected bind rejection, and peer unreachable handling. Source review narrowed the root cause to `CheckSocketEncrypted()` using the business type returned by `ClientGetChannelIdAndTypeBySocketId()` where `GetEncryptByChannelId()` expects channel type.
- 2026-07-06 protected signed SROS2 GREEN refresh: after changing the DSoftBus check to query the real channel type with `ClientGetChannelBySessionId()`, the RK3588A focused `MddsDSoftBusBackendTest --gtest_filter="MddsConnManagerTest.ProtectedTransport*"` passed 7/7 including `ProtectedTransportUsesChannelTypeForEncryptionLookup`, and full `MddsDSoftBusBackendTest` passed 140/140. The bridge was rebuilt/deployed with sha `330a61f59bf3b6e1dbe4198562b9081b0474701b61cab08b33d1631cb5f99698`; domain 222 `run_cross_board_rmw_mdds_sros2_protected.sh` then passed signed policy and protected transport activation on both boards, authorized protected pub/sub `received=58`, unauthorized publish denied, and final `cross_board_rmw_mdds_sros2_protected_ok`. The broader security goal still needs permissive/enforce combinations, tampered/missing credential board matrices, soak, sanitizer, and performance evidence before production certification.
- 2026-07-06 late status sync: after the protected signed SROS2 GREEN refresh, later rmw_mdds graph-refresh work updated the current deployed rmw/broker artifacts to `librmw_mdds_cpp.so` sha `4459fbe764d98775c91c5505c20730560866e7cfacaf44f5d6ca904b372f9386` and broker sha `7af506a2775937544b95bfee3d63d5a0e8f7aff2949aa5870c2497ac422f34db` on both boards; the protected bridge/probe/softbus-client hashes from the security run are unchanged. The current rclpy graph-cleanup artifacts are also deployed with `type_description_service.py` sha `344bd340293bf65bf998b0a2fed3b00cc344e2ad3865db7ee8f9be98afa4deda` and `_rclpy_pybind11` sha `de2a4f7ca198aeda71dca0f9b32ee3d75706a8cf645be158ee8d75757ba1c6f8`, but that does not change the security evidence. The domain-222 protected SROS2 PASS was not rerun after these later graph-refresh builds, so it remains evidence for the protected harness at that checkpoint, while the current board deployment hashes are the refreshed values above.
- 2026-07-07 security status-doc sync: the latest current-board rmw/broker deployment is now the late IPC overlay, `librmw_mdds_cpp.so=e347fe583811c9e3082476b804bae5a638f553756b1e27ef5d2e9e75225413b6` and `rmw_mdds_broker=fb70c341e896fcf45a7341b9732edc1bdb4f7ffc6cc1a8051fd1c16c5cc6abab`; bridge `8b918aa3592249fb6c922c490c92566f32d77311d18104d64496059677e578bc`, softbus client `e1771298eeb820dac59f762ca586a96e2c538e70350bc1d021f4b3d3b76aff9d`, and protected probe `6aa06d13089af31f93e5ed1c7c694fc86fa353077266d8b2003be9481cb063c0` remain deployed on both boards. The lightweight contracts still pass (`bridge_protected_transport_contract_ok`, `rmw_mdds_script_contracts_ok`), but the protected signed SROS2 board harness was not rerun after the late IPC overlay. Treat domain 290 as the latest protected-harness PASS artifact for the recorded service-stress repair overlay, not as fresh production security certification for every later overlay.
- 2026-07-07 MDDS bridge refresh caveat: a later large-payload fix rebuilt and deployed `libmdds_bridge_shared.z.so=037553404f7a9d9a37ad69343f0f8a013ec66230d0da53af1c177a3ad55c18ef` to both boards while `libsoftbus_client.z.so` remained `e1771298eeb820dac59f762ca586a96e2c538e70350bc1d021f4b3d3b76aff9d`. This refresh was validated by focused MDDS large-payload units and a 16MiB exact-total topic run, not by a fresh protected signed SROS2 board harness. Therefore domain 290 remains the latest protected-harness PASS artifact for the earlier recorded bridge/rmw overlay; do not promote it to production security certification for the current large-payload bridge without rerunning the protected harness.
