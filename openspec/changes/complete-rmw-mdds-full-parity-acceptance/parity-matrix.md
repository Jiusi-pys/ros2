# rmw_mdds Full Parity Acceptance Matrix

Audit date: 2026-07-04
Change: `complete-rmw-mdds-full-parity-acceptance`

This matrix is the current acceptance boundary for the broad "perfect/all ROS 2 middleware features" goal. A `proven` row has current code, test, contract, or board evidence. An `incomplete` row blocks goal completion until it has a RED test/contract, implementation, runtime evidence, or explicit user acceptance as out of scope. This audit does not accept any unsupported row as out of scope yet.

| Surface | Current evidence source | Classification | Minimum RED test or executable contract if incomplete |
| --- | --- | --- | --- |
| Init and context lifecycle | `rmw_init.cpp` initializes context, RTPS/broker mode, and security policy; `test_lifecycle` and upstream/package CTest are recorded in `complete-rmw-mdds-feature-closure/tasks.md` and `complete-rmw-mdds-zero-copy-security/tasks.md`. | proven | None. Keep package CTest and upstream `test_rmw_implementation` gates. |
| Nodes | `rmw_node.cpp` creates/destroys nodes and graph guard conditions; `test_lifecycle` covers context/node/guard lifecycle; board doctor reported `middleware name : rmw_mdds_cpp`. | proven | None. Keep lifecycle and board doctor gates. |
| Wait sets | `rmw_wait.cpp` handles subscriptions, guards, services, clients, and events; `test_service_inproc` covers service-ready wait behavior and `test_lifecycle` covers guard readiness consumption. | proven | None. Keep focused wait/lifecycle tests. |
| Guard conditions | `rmw_guard_condition.cpp` implements create/destroy/trigger; `test_lifecycle` covers graph guard and explicit guard triggering through `rmw_wait`. | proven | None. Keep lifecycle guard tests. |
| Graph APIs | Symbol-surface test exports graph APIs; `test_graph` covers topic/node endpoint info, GIDs, QoS, type hashes, and enclaves; host CLI graph probe is included in `ohos/test_rmw_mdds_delivery_contracts.sh`. | proven | None. Keep `test_graph` and host CLI graph probe. |
| Pub/sub typed APIs | `rmw_create_publisher`, `rmw_create_subscription`, `rmw_publish`, `rmw_take`, sequence take, and message-info paths are implemented; package CTest, host CLI pub/sub/message-info, native M2M, and gateway matrix evidence are recorded in prior task files. | proven | None. Keep package CTest, host CLI pub/sub, message-info, native M2M, and gateway matrix lanes. |
| Service APIs | `rmw_create_service`, `rmw_create_client`, `rmw_send_request`, `rmw_take_request`, `rmw_send_response`, `rmw_take_response`, and availability checks are implemented; `test_service_inproc` covers AddTwoInts, Trigger, array service payloads, and wait behavior; host CLI and board/gateway service lanes passed. | proven | None. Keep `test_service_inproc`, host CLI service, native M2M service, and gateway service lanes. |
| Client APIs | Client creation, request send, response take, GID, actual QoS, callbacks, and service availability are covered by service code paths and `test_service_inproc`; gateway service evidence verifies cross-RMW client behavior. | proven | None. Keep service/client focused tests and gateway service lane. |
| Actions | ROS 2 actions are exercised over their RMW service/topic primitives: host CLI Fibonacci action, native dual-board M2M Fibonacci action, and gateway action with send_goal/get_result counters, feedback/status movement, accepted goal, exact result sequence, and `SUCCEEDED` markers. | proven | None. Keep host CLI action, native M2M action, and gateway action lanes. |
| Serialized APIs | `rmw_publish_serialized_message`, `rmw_take_serialized_message`, serialize/deserialize, and serialized message sizing are exported; `test_bridge_backend` covers serialized bridge publish/take and `test_rmw_implementation` passed. | proven | None. Keep serialized bridge and upstream conformance tests. |
| Dynamic APIs | `rmw_take_dynamic_message` and `rmw_take_dynamic_message_with_info` delegate through serialized take and `rosidl_dynamic_typesupport_dynamic_data_deserialize`; dynamic tests are recorded in `complete-rmw-mdds-feature-closure/tasks.md`. | proven | None for current RMW dynamic-take API. Keep dynamic take host tests and bridge-loaned dynamic take regression. |
| Generated message type breadth | `MessageAdapter::Init` prefers generic CDR callbacks and keeps introspection metadata; tests include String, Int32, Int32MultiArray, nested fields, and service array payloads. | proven | None for current representative coverage. Add more conformance fixtures if a new ROS IDL shape fails. |
| Loaned messages: fixed-size raw shape | `rmw_borrow_loaned_message`, `rmw_publish_loaned_message`, `rmw_take_loaned_message_with_info`, and return paths require raw fixed-size bridge loans; `ohos/test_rmw_mdds_zero_copy_contracts.sh` rejects encode/decode/copy fallbacks; build-space loaned tests passed 9/9 + 9/9. | proven | None for fixed-size raw loaned shape. Keep zero-copy contract and bridge loaned tests. |
| Loaned messages: unbounded, sequence, dynamic, or filtered shapes | `MddsLoanArena` now provides aligned bridge-loan segments and a guarded active-loan allocation hook for generated C++ dynamic member storage. `rmw_borrow_loaned_message()` advertises/returns bridge-backed publisher loans for the covered unbounded string, dynamic sequence, and nested dynamic shapes; `MessageAdapter::DynamicStorageWithinLoan()` validates dynamic storage ownership, and `rmw_publish_loaned_message()` publishes the same bridge loan only after `MessageAdapter::PrepareLoanedDynamicMddsPayload()` succeeds. `ohos/test_rmw_mdds_zero_copy_contracts.sh` rejects `EncodeMddsIntoBuffer` and second-loan copy fallback in the RMW loaned-publish body and now emits `rmw_mdds_zero_copy_contracts_ok`. `test_bridge_loaned_rmw` passes 9/9 normally, the enabled full-parity loaned-shape group passes 3/3, and `ohos/test_rmw_mdds_full_parity_red_contracts.sh` reports `RESULT|rmw_mdds_full_parity_loaned_shapes|PASS`. | proven | None for the approved host dynamic-loan shapes. Add a new RED contract before expanding beyond the covered dynamic string/sequence/nested message shapes or claiming board SHM copy-count evidence. |
| QoS compatibility | `rmw_qos_profile_check_compatible` delegates to `rmw_dds_common`; `test_qos` and host CLI QoS/transient-local probes are included in delivery contracts. | proven | None. Keep QoS unit and CLI probes. |
| QoS and RMW events | Matched, deadline, liveliness, incompatible QoS, incompatible type, and message-lost events are enforced; `rmw_event_type_is_supported` returns supported for enforced events; `test_event` covers 41 event/content-filter cases. | proven | None. Keep full `test_event`. |
| Content filters | `rmw_subscription_set_content_filter` and get APIs delegate to broker filter logic; `test_pubsub_inproc` and `test_event` cover string/numeric filters, nested fields, parameters, LIKE/IN/BETWEEN, boolean grouping, unary NOT, and filtered delivery. | proven | None. Keep content-filter unit tests. |
| Type identity and RIHS | `MessageAdapter::TypeHash`, IPC protocol type-hash fields, `test_graph`, `test_identity`, and `run_rmw_mdds_type_description_probe.sh` cover endpoint type hashes, GIDs, enclaves, and `RIHS01_` type-description service for `std_msgs/msg/String`. | proven | None. Keep graph/identity tests and type-description probe. |
| Security: fail-closed and local SROS2 policy | `LoadSecurityPolicy` requires readable `governance.xml` and `permissions.xml` in enforce mode, records publish/subscribe grants, and publisher/subscription creation checks topic access; host SROS2 policy contract and board SROS2 policy harness passed. | proven | None for local policy enforcement. Keep fail-closed, host policy, and board policy contracts. |
| Security: signed artifacts, governance semantics, and transport protection | Current policy handling preserves local XML grants for `NONE` protection kinds and validates protected signed artifacts before accepting protected governance. `LoadSecurityPolicy()` verifies `governance.xml.sig` and `permissions.xml.sig` as RSA/SHA-256 detached signatures against a trusted `permissions_ca.cert.pem`, validates `identity.pem` against `identity_ca.cert.pem`, rejects tampered signed permissions, rejects signed identity mismatch through permissions `subject_name`, rejects unsigned protected governance, and requires an authenticated plus encrypted protected-transport gate before protected endpoint creation. On 2026-07-04, host full-parity contracts reported `RESULT|rmw_mdds_full_parity_signed_security|PASS`; the protected board harness reported signed-policy PASS and `RESULT|board_sros2_protected_transport|PASS|authenticated=1|encrypted=1` on both RK3588/KaihongOS boards, authorized protected pub/sub PASS with `received=20`, unauthorized protected publish PASS with `denied`, and `cross_board_rmw_mdds_sros2_protected_ok`. | proven | None. Keep host signed-security contracts and the protected SROS2 board harness. |
| Network-flow metadata: direct RTPS | `rmw_publisher_get_network_flow_endpoints` and subscription equivalent report the direct RTPS user-data UDP endpoint when an RTPS participant exists; `NetworkFlowEndpointsReportDirectRtpsUserDataPort` covers this path. | proven | None for direct RTPS. Keep network-flow unit test. |
| Network-flow metadata: broker or MDDS bridge mode | In broker mode `rmw_init` skips the RTPS participant, and `rmw_publisher_get_network_flow_endpoints` plus `rmw_subscription_get_network_flow_endpoints` now report one explicit non-IP broker endpoint with unknown transport/protocol, zero port, and `rmw_mdds_broker` address metadata. `ohos/test_rmw_mdds_full_parity_red_contracts.sh` reports `RESULT|rmw_mdds_full_parity_broker_network_flow|PASS`; direct RTPS metadata remains covered separately. | proven | None for broker local IPC metadata. If future MDDS/DSoftBus transport exposes IP-like endpoint data, add a transport-specific endpoint contract instead of overloading the broker metadata row. |
| Logging | `rmw_set_log_severity` accepts defined severities and rejects unknown values; `test_logging` covers both paths. | proven | None. Keep `test_logging`. |
| Host delivery | `ohos/test_rmw_mdds_delivery_contracts.sh` requires security, SROS2 policy, zero-copy, artifact, package CTest, upstream `test_rmw_implementation`, type-description, and host CLI pub/sub/service/action/params/lifecycle/graph/QoS/transient-local/message-info markers. On 2026-07-03, after the current security, broker network-flow, dynamic-loaned-message, and CLI harness robustness changes, the contract emitted `rmw_mdds_delivery_contracts_ok`, with package CTest `23/23`, upstream `test_rmw_implementation` `16/16`, and every listed host CLI marker PASS. The affected full-parity contract also emitted PASS for loaned shapes, signed security, and broker network-flow. | proven | None for current scoped host state. Re-run after any future behavior change or board overlay refresh. |
| Board delivery | On 2026-07-04, the refreshed OHOS `rmw_mdds_cpp` overlay was rebuilt with target OpenSSL enabled and deployed to boards `3e01ff55454d202020104033bf453b00` and `3e01ff55454d202020104433991c3b00` with `librmw_mdds_cpp.so` sha `b08e129c4086b3593cf1271da6a2e2acc853a7c6b98e2f6f265995d081dbf1b1`, broker sha `83b93da71c72133d89017c52cd95e5dc06c2d661626123bb94553eaa3d8be970`, and bridge sha `771cec930859b407dc7c48aa94a0fb4b342478bb6403abaf72974fbb2d86e9c1`. Native M2M pub/sub/service/action emitted `M2M_SUMMARY pass=3 fail=0`, the gateway matrix emitted `MATRIX_SUMMARY pass=8 fail=0`, gateway service/action/params/lifecycle emitted PASS markers, and protected signed SROS2 emitted the signed-policy, authenticated/encrypted transport, authorized traffic, and denied traffic markers. | proven | None for current scoped state. Rebuild, deploy, and re-run affected board lanes after any future parity implementation. |
| Delivery handoff | Local handoff is the executed endpoint for the current request because no push, PR, Gerrit submission, or OpenSpec archive was requested. DSoftBus/MDDS is committed locally on `mdds-claude` at `e2f3d7e9a` (`feat(mdds): 完成零拷贝 loaned samples 与桥接验证`), and ROS 2/rmw_mdds implementation is committed locally on `jazzy-ubuntu-20.04` at `86ab375` (`feat(rmw_mdds): complete MDDS parity board closure`). This OpenSpec evidence update records that local handoff as the selected delivery endpoint without touching remotes. | proven | None for the current local handoff endpoint. If a later remote/archive delivery is requested, execute and record that separate endpoint. |

## Current Completion Decision

The full persistent goal is finished for the current local handoff endpoint. The ROS 2 over `rmw_mdds_cpp` and MDDS rows in this matrix are proven by host plus RK3588/KaihongOS board evidence, including dynamic loaned shapes, signed SROS2 artifacts, authenticated/encrypted protected transport, native MDDS lanes, gateway lanes, and DSoftBus/MDDS test builds. The selected delivery endpoint is local handoff with the commits recorded above; no push, PR, Gerrit submission, or OpenSpec archive was requested.
